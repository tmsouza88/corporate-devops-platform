apiVersion: v1
kind: Service
metadata:
  name: hello-app # MUDANÇA AQUI: de hello-service para hello-app
  namespace: default
spec:
  selector:
    app: hello-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678
  type: ClusterIP