apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: applications
  namespace: flux-system
spec:
  interval: 5m0s
  path: ./clusters/kind-cluster/applications
  prune: true # Se você deletar no Git, o Flux deleta no K8s
  sourceRef:
    kind: GitRepository
    name: flux-system